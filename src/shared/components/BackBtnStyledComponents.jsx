import styled from "styled-components";

export const BackBtn = styled.button`
    padding: 0.2rem;
    border-radius: 5px;
    background-color: transparent;
    cursor: pointer;
    width: fit-content;
    height: fit-content;
    outline: none;
`